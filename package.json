{
  "name": "reynikg-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Robert Reynik portfolio — static site with a generator that builds a case-study page per project from data/works.js.",
  "scripts": {
    "build": "node build/generate.js && node build/generate-articles.js && node build/generate-index.js && node build/generate-sitemap.js",
    "build:projects": "node build/generate.js",
    "build:articles": "node build/generate-articles.js",
    "build:index": "node build/generate-index.js",
    "build:sitemap": "node build/generate-sitemap.js",
    "optimize": "node build/optimize-images.js"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
